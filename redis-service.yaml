apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  ports:
  - port: 6379
    targetPort: 6379
  clusterIP: None  # Робить сервіс внутрішнім для StatefulSet
  selector:
    app: redis